describe("buffer_from_file", () => {
  it("must get the original file bytes", async () => {
    // const fileBytes = [103, 117, 101, 115, 97, 110, 116];
    // const file = new File([new Int8Array(fileBytes)], "exemplo");
    // expect(await buffer_from_file(file as any)).toEqual(
    //   new Int8Array(fileBytes)
    // );
  });
});
